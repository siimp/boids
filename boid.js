"use strict";

class Boid {

  constructor(x, y, angle, speedX, speedY) {
    this.x = x || 0;
    this.y = y || 0;
    this.angle = angle || 0;
    this.speedX = speedX || 0;
    this.speedY = speedY || 0;
  }

}
